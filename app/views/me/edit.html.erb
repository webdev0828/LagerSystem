<h1>Rediger brugerkonto</h1>

<% if @user.errors.any? %>
  <div class="form-errors">
    <% @user.errors.full_messages.each do |msg| %>
      <%= msg %><br>
    <% end %>
  </div>
<% end %>

<div class="form_container login">
<%= form_for(@user, :url => me_path, :html => { :method => :put }) do |f| %>
  <table class="form">
    <tr style="border-bottom:1px solid #ddd;">
      <td><%= f.label :current_password %></td>
      <td><%= f.password_field :current_password %></td>
    </tr>
    <tr>
      <td><%= f.label :username %></td>
      <td><%= f.text_field :username %></td>
    </tr>
    <tr>
      <td><%= f.label :full_name %></td>
      <td><%= f.text_field :full_name %></td>
    </tr>
    <tr>
      <td><%= f.label :email %></td>
      <td><%= f.text_field :email %></td>
    </tr>
    <tr>
      <td><%= f.label :phone %></td>
      <td><%= f.text_field :phone %></td>
    </tr>
    <tr>
      <td><%= f.label :password %> </td>
      <td><%= f.password_field :password %></td>
    </tr>
    <tr>
      <td><%= f.label :password_confirmation %></td>
      <td><%= f.password_field :password_confirmation %></td>
    </tr>
    <tr>
      <td></td>
      <td class="submit"><%= f.submit "Opdater" %> &nbsp; eller <%= link_to 'annullér', root_path %></td>
    </tr>
  </table>
<% end %>
</div>
